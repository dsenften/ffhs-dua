= Erste Schritte mit ALGS4
:author: Daniel Senften
:revdate: {docdate}
:toc: left
:toclevels: 3
:encoding: utf-8
:toc-title: Inhaltsverzeichnis
:source-highlighter: highlight.js
:icons: font
:numbered:

== Installation

=== Repository klonen

[source,bash]
----
git clone <repository-url>
cd ffhs-dua
----

=== Entwicklungsumgebung einrichten

[source,bash]
----
# Mit uv (empfohlen)
uv sync

# Oder mit setup-Skript
python scripts/setup_dev.py
----

== Ihr erstes Programm

=== Stack-Beispiel

[source,python]
----
from src.algs4.fundamentals.stack import Stack

# Stack erstellen
stack = Stack[str]()

# Elemente hinzufügen
stack.push("Erstes Element")
stack.push("Zweites Element")

# Element entfernen
element = stack.pop()
print(f"Entfernt: {element}")  # "Zweites Element"
----

=== Quick Sort-Beispiel

[source,python]
----
from src.algs4.sorting.quick import Quick

# Liste sortieren
numbers = [64, 34, 25, 12, 22, 11, 90]
print(f"Unsortiert: {numbers}")

sorted_numbers = Quick.sort(numbers)
print(f"Sortiert: {sorted_numbers}")

# Überprüfung
print(f"Ist sortiert: {Quick.is_sorted(sorted_numbers)}")
----

=== Heap Sort-Beispiel

[source,python]
----
from src.algs4.sorting.heap import Heap

# Liste sortieren (garantierte O(n log n) Performance)
numbers = [64, 34, 25, 12, 22, 11, 90]
print(f"Unsortiert: {numbers}")

sorted_numbers = Heap.sort(numbers)
print(f"Sortiert: {sorted_numbers}")

# Überprüfung
print(f"Ist sortiert: {Heap.is_sorted(sorted_numbers)}")

# Heap Sort ist besonders gut für Worst-Case-Garantien
worst_case = list(range(100, 0, -1))  # Umgekehrt sortiert
Heap.sort(worst_case)  # Immer noch O(n log n)!
----

=== Union-Find-Beispiel

[source,python]
----
from src.algs4.fundamentals.uf import UF

# Union-Find mit 10 Elementen erstellen
uf = UF(10)

# Verbindungen erstellen
uf.union(0, 1)
uf.union(2, 3)
uf.union(1, 2)

# Konnektivität prüfen
print(f"0 und 3 verbunden: {uf.connected(0, 3)}")  # True
print(f"0 und 4 verbunden: {uf.connected(0, 4)}")  # False
print(f"Anzahl Komponenten: {uf.count()}")
----

=== Test schreiben

[source,python]
----
# tests/test_mein_stack.py
from src.algs4.fundamentals.stack import Stack

def test_mein_stack():
    stack = Stack[int]()
    stack.push(42)
    assert stack.pop() == 42
    assert stack.is_empty()
----

=== Test ausführen

[source,bash]
----
uv run pytest tests/test_mein_stack.py -v
----

== TDD-Workflow

. **Test schreiben** (rot)
. **Minimale Implementierung** (grün)
. **Code verbessern** (refactor)
. **Wiederholen**

== Nächste Schritte

=== Dokumentation erkunden
- **link:../index.adoc[Hauptdokumentation]**: Vollständige Projektübersicht
- **link:../api/fundamentals.adoc[Fundamentals API]**: API-Referenz für Grundlagen
- **link:../api/sorting.adoc[Sorting API]**: API-Referenz für Sortieralgorithmen
- **link:../quick_sort_documentation.adoc[Quick Sort]**: Detaillierte Algorithmus-Dokumentation

=== Praktische Übungen
- Erkunden Sie die Jupyter Notebooks in `notebooks/`
- Führen Sie Performance-Tests aus: `python benchmarks/sorting_benchmarks.py`
- Experimentieren Sie mit verschiedenen Datengrössen und -typen

=== Erweiterte Themen
- **link:../komplexitaetsanalyse.adoc[Komplexitätsanalyse]**: Verstehen Sie die Performance-Charakteristika
- **link:../union_find_documentation.adoc[Union-Find]**: Lernen Sie über dynamische Konnektivität
- **link:../heap_sort_documentation.adoc[Heap Sort]**: Detaillierte Heap-Sort-Dokumentation
- **Test-Dokumentation**: Verstehen Sie die umfassenden Test-Strategien